// drop down menu in html
// allow user to click each item from drop down
// when item is selected from drop down display a prompt that accepts a number type input.
// store that user number in an array
// create a button called Add to Cart
// allow user to select Add to Cart submit button 
// store user input into a variable
// clear the individual input numbers for each image but still keep the total number variable.
// create item constructor with filepath and name  (use the constructor we already have).

// Item.allItems = [];

// Item.numberOfEachItem = 0;
// var itemName = [];
// var itemQuantity = [];

function itemsHaveBeenAdded() {
  // Get the snackbar DIV
  document.getElementById("add-to-cart").innerHTML = 'Items have been added to cart!';

}



// function Item(filePath, nameOfItem) {
//   this.filePath = filePath;
//   this.nameOfItem = nameOfItem;
//   this. = avgCookiesSold;
//   this.maxCustomers = maxCustomers;
//   this.salesPerEachHour = [];
//   this.totalCookies = 0;
  

// function addToCart(event) {
//   console.log('add item to cart');
//   event.preventDefault();
//   console.log(event.target.storeName.value);
//   var newStoreName = event.target.storeName.value;
//   var newMinCustomers = event.target.minCustomers.value;
//   var newMaxCustomers = event.target.maxCustomers.value;
//   var newAvgCookiesSold = event.target.avgCookiesSold.value;

//   new Store(newStoreName, newMinCustomers, newMaxCustomers, newAvgCookiesSold);

//   storeTable.innerHTML = '';
//   makeHeaderRow();
//   renderAllStores();
//   totalCookieFooter();
// }

// function itemQuantity() {
//   var numberOfItems = parseInt(prompt('Please input the desired quantity'));
//   console.log('Desired Quantity ' + itemQuantity);
// }

